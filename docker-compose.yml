services:
  pixo-display:
    image: ghcr.io/emilw/pixo-display:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - PIXOO_IP=192.168.10.139
      - TZ=Europe/Stockholm
    volumes:
      - /volume1/docker/pixo-display:/config:ro
